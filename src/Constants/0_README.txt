Actually, we can not declare the Constants in EasyLanguage.
However, we could use the 'function' to define the Constants.

This function is not input and it must be a name with upper-case.
For example:

OPEN_TIME = 930;

